[workspace]
resolver = "2"
members = ["crates/ringi"]

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.88"
license = "MIT OR Apache-2.0"
repository = "https://github.com/tacticaldoll/ringi"

[workspace.dependencies]
# Published family facades composed by ringi: pacta owns lifecycle, suunta owns convergence,
# shaahid owns idempotency, and cadw owns residual-ledger batch-fold validation.
pacta = "0.3.0"
pacta-memory = "0.3.0"
pacta-conformance = "0.3.0"
rusqlite = { version = "0.32", features = ["bundled"] }
suunta = "0.1.1"
shaahid = "0.1.1"
cadw = "0.1.0"
uuid = { version = "1", features = ["v5"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
wait-timeout = "0.2"
